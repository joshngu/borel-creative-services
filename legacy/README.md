Legacy static files from the pre-Next.js version are stored here for reference.

- `index.html`
- `script.js`

Active app files now live in:
- `app/`
- `public/assets/`
- `styles.css` (global stylesheet imported by `app/layout.tsx`)
